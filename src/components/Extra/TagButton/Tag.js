import React from 'react';
import "./Tag.css"

const Tag = (props) => {
    return (

        <div className=" Previlage">
        <h2 className=" Color">{props.name}</h2>

    </div>
    );
}

export default Tag;
